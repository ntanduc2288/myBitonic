import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 10,
        marginBottom: 10
    },
    item: {
        fontWeight: 'bold',
        fontSize: 20
    }
})

export default styles;